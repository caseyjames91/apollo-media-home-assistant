# Apollo Media 0.9.54 — Versioned Show Row Episode Context

- Carries forward the show-row episode-context fix from the immediately previous build.
- Show rows can expose the latest/relevant episode context for card presentation.
- Local Jellyfin show enrichment remains batched to avoid per-show request slowdown.
- Popular/trending show enrichment preserves the existing discovery paths.
- No playback, resume, Try Next, active-media, provider, or Home Assistant routing behavior changed.
- This build establishes the corrected, consistently versioned artifact set at 0.9.54.
