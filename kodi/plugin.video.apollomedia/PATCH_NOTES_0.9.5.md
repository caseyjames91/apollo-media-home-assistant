# Apollo Media 0.9.5

- Removes the final legacy `IN LIBRARY` episode label.
- Uses the same subtle gray dot (`•`) for local movies, shows, seasons, and episodes.
- No playback, metadata, source-ranking, compatibility, or routing behavior changed.
- Marks local/remote playback routing validation as passed based on user testing.
